# All the global keywords and some vars to make sure we distinguish

-- src.http --
@timeout = 30s
@connection-timeout = 10s
@no-redirect
@normal-var = test
-- want.yaml --
name: global-keywords.txtar
vars:
  normal-var: test
timeout: 30s
connectionTimeout: 10s
noRedirect: true
